#spring.application.name=authService

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/servicoDeAutenticacao?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
